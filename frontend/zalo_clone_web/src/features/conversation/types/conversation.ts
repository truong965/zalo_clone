/**
 * Re-export shim — canonical definitions moved to src/types/api in Phase 1.
 * Kept for backwards-compat so older deep imports still resolve.
 */
export type { ConversationUI, ConversationListItemUI } from '@/types/api';
