/**
 * Conversation Module Constants
 *
 * Centralized business rule constants to avoid magic numbers.
 */

/** Maximum number of conversations a user can pin */
export const MAX_PINNED_CONVERSATIONS = 5;

/** Maximum number of messages that can be pinned per conversation */
export const MAX_PINNED_MESSAGES = 10;
